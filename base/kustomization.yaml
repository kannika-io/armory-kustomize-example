helmCharts:
  - name: kannika
    includeCRDs: false
    valuesInline:
      global:
        kubernetes:
          namespace: kannika-data
      kubeVersionOverride: 1.28.0
    releaseName: kannika
    version: 0.12.4
    repo: oci://quay.io/kannika/charts
